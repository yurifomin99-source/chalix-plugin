name: ci_tutor_k8s

on:
  push:
    branches: [ "main" ]
    paths:
      - 'test-ci/**'

jobs:
  refresh-tutor-k8s:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run command in subdirectory
        run: touch test.txt
        working-directory: /home/cicd/test.txt